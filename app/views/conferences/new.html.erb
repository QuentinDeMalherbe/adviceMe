<div class="navbar">
<p>coucou je suis une navbar</p>
<%= link_to 'créer son vice', new_vice_path %>
</div>


<div class="container">
    <%= simple_form_for([@vice, @conference]) do |form| %>
    <%= form.input :title %>
    <%= form.input :date %>
    <%= form.button :submit %>
  <% end %>
</div>
